
import relicLookup from '../jsons/relicPriceLookup.json' with {type: 'json'};
import sortedRelicLookup from '../jsons/relicPriceLookup.json' with {type: 'json'};

// return best 30 relics to upgrade, where the difference between radiant and intact is the smallest
// returns quantity of each
export async function getBestRelicsToUpgrade(){

    return bestRelics;
}